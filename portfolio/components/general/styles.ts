// 

export enum buttonColours {
    primary = 'bg-acid-500',
}

/**
 * Enum containing the class tags for background colours 
 */
export enum backgrounds {
    navigation = 'bg-linear-to-r from-fern-300 to-fern-500',
    card = 'bg-linear-to-br from-sulphur-200 to-sulphur-300',
    hyperlink = '',
    page = "bg-linear-to-b from-cream-50 to-cream-100"
}

export enum borders {
    fern = "rounded-lg border-5 border-fern-400",
    shadedFern = 'rounded-lg border-5 border-fern-400 shadow-[5px_5px_0px_0px] shadow-fern-500',
}

/**
 * Enum containing class tags for text styles
 */
export enum text {
    default = "font-atkinson text-black",
    h1 = "font-sedgwick-display text-3xl",
    h2 = "font-sedgwick text-2xl",
    h3 = "font-atkinson text-black text-xl font-semibold",
    footerHeader = "font-atkinson text-black text-xl",
    italic = "font-atkinson italic"
}